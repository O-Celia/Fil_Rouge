# service:
#   type: LoadBalancer

# grafana:
#   admin:
#     existingSecret: "grafana-password"
#     userKey: admin-user
#     passwordKey: admin-password

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://prometheus-kube-prometheus-prometheus:9090
      access: proxy
      isDefault: true
    - name: Loki
      type: loki
      url: http://loki-backend:3100
      access: proxy

# persistence:
#   enabled: true
#   type: pvc
#   accessModes:
#     - ReadWriteOnce
#   size: 10Gi

dashboards:
  default:
    my-dashboard:
      file: dashboard.json
